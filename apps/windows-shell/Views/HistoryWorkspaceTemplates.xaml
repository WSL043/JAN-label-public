<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:JanLabel.WindowsShell">
  <DataTemplate DataType="{x:Type local:HistoryWorkspaceModel}">
    <DockPanel LastChildFill="True">
      <Border DockPanel.Dock="Bottom" Background="#E6EBF1" BorderBrush="#BCC7D2" BorderThickness="1,1,0,0">
        <Grid Margin="8,6">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.25*" />
            <ColumnDefinition Width="1*" />
          </Grid.ColumnDefinitions>
          <Border Grid.Column="0" Style="{StaticResource PaneBorderStyle}" Margin="0,0,8,0">
            <StackPanel>
              <DockPanel>
                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Review / Export Messages" />
                <TextBlock DockPanel.Dock="Right" Foreground="{StaticResource MutedBrush}" Text="{Binding MessageSummary}" />
              </DockPanel>
              <DataGrid Style="{StaticResource OperatorDataGridStyle}" ItemsSource="{Binding MessageRows}">
                <DataGrid.Columns>
                  <DataGridTextColumn Width="70" Binding="{Binding Level}" Header="Level" />
                  <DataGridTextColumn Width="90" Binding="{Binding Source}" Header="Source" />
                  <DataGridTextColumn Width="*" Binding="{Binding Message}" Header="Message" />
                </DataGrid.Columns>
              </DataGrid>
            </StackPanel>
          </Border>
          <Border Grid.Column="1" Style="{StaticResource PaneBorderStyle}" Margin="0">
            <StackPanel>
              <DockPanel>
                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Retention / Restore Status" />
                <TextBlock DockPanel.Dock="Right" Foreground="{StaticResource MutedBrush}" Text="{Binding FooterDetail}" />
              </DockPanel>
              <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding StatusItems}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Margin="0,0,0,6" Padding="8" Background="#FCFDFE" BorderBrush="#CFD8E1" BorderThickness="1">
                      <DockPanel>
                        <StackPanel>
                          <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding Label}" />
                          <TextBlock FontWeight="SemiBold" Text="{Binding Value}" />
                          <TextBlock FontSize="11" Foreground="{StaticResource MutedBrush}" Text="{Binding Detail}" />
                        </StackPanel>
                        <TextBlock DockPanel.Dock="Right" Foreground="{Binding Accent}" FontWeight="SemiBold" Text="{Binding Tone}" />
                      </DockPanel>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </Border>
        </Grid>
      </Border>

      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="320" MinWidth="260" />
          <ColumnDefinition Width="6" />
          <ColumnDefinition Width="*" MinWidth="420" />
          <ColumnDefinition Width="6" />
          <ColumnDefinition Width="400" MinWidth="320" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource PaneBorderStyle}" Margin="0,0,8,0">
          <TabControl Style="{StaticResource WorksurfaceTabControlStyle}">
            <TabItem Header="Pending Proofs">
              <Grid Margin="10">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel>
                  <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Pending Proof Review" />
                  <TextBlock DockPanel.Dock="Right" Foreground="{StaticResource MutedBrush}" Text="{Binding PendingProofSummary}" />
                </DockPanel>
                <DataGrid
                    Grid.Row="1"
                    Style="{StaticResource OperatorDataGridStyle}"
                    ItemsSource="{Binding PendingProofs}"
                    SelectedItem="{Binding SelectedPendingProof, Mode=TwoWay}">
                  <DataGrid.Columns>
                    <DataGridTextColumn Width="170" Binding="{Binding Label}" Header="Proof" />
                    <DataGridTextColumn Width="90" Binding="{Binding Badge}" Header="State" />
                    <DataGridTextColumn Width="150" Binding="{Binding Subtext}" Header="Subject" />
                    <DataGridTextColumn Width="*" Binding="{Binding Detail}" Header="Operator note" />
                  </DataGrid.Columns>
                </DataGrid>
              </Grid>
            </TabItem>
            <TabItem Header="Backup Bundles">
              <Grid Margin="10">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel>
                  <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Backup Bundles" />
                  <TextBlock DockPanel.Dock="Right" Foreground="{StaticResource MutedBrush}" Text="{Binding BundleSummary}" />
                </DockPanel>
                <DataGrid
                    Grid.Row="1"
                    Style="{StaticResource OperatorDataGridStyle}"
                    ItemsSource="{Binding BundleRows}"
                    SelectedItem="{Binding SelectedBundle, Mode=TwoWay}">
                  <DataGrid.Columns>
                    <DataGridTextColumn Width="180" Binding="{Binding Label}" Header="Bundle" />
                    <DataGridTextColumn Width="90" Binding="{Binding Badge}" Header="State" />
                    <DataGridTextColumn Width="140" Binding="{Binding Subtext}" Header="Scope" />
                    <DataGridTextColumn Width="*" Binding="{Binding Note}" Header="Operator note" />
                  </DataGrid.Columns>
                </DataGrid>
              </Grid>
            </TabItem>
          </TabControl>
        </Border>

        <GridSplitter Grid.Column="1" Style="{StaticResource PaneGridSplitterStyle}" ResizeDirection="Columns" />

        <Border Grid.Column="2" Style="{StaticResource PaneBorderStyle}" Margin="0,0,8,0">
          <TabControl Style="{StaticResource WorksurfaceTabControlStyle}">
            <TabItem Header="Audit Ledger">
              <Grid Margin="10">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel>
                  <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Audit Ledger" />
                  <TextBlock DockPanel.Dock="Right" Foreground="{StaticResource MutedBrush}" Text="{Binding LedgerSummary}" />
                </DockPanel>
                <DataGrid
                    Grid.Row="1"
                    Style="{StaticResource OperatorDataGridStyle}"
                    ItemsSource="{Binding AuditRows}"
                    SelectedItem="{Binding SelectedAuditRow, Mode=TwoWay}">
                  <DataGrid.Columns>
                    <DataGridTextColumn Width="85" Binding="{Binding Time}" Header="Time" />
                    <DataGridTextColumn Width="90" Binding="{Binding Lane}" Header="Lane" />
                    <DataGridTextColumn Width="160" Binding="{Binding Subject}" Header="Subject" />
                    <DataGridTextColumn Width="90" Binding="{Binding Status}" Header="State" />
                    <DataGridTextColumn Width="170" Binding="{Binding Lineage}" Header="Lineage" />
                    <DataGridTextColumn Width="*" Binding="{Binding Note}" Header="Operator note" />
                  </DataGrid.Columns>
                </DataGrid>
              </Grid>
            </TabItem>
            <TabItem Header="Filters / Export">
              <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                  <DockPanel>
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Audit Filters / Exports" />
                    <TextBlock DockPanel.Dock="Right" Foreground="{StaticResource MutedBrush}" Text="{Binding FilterSummary}" />
                  </DockPanel>
                  <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding FilterRows}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Grid Margin="0,0,0,6">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Foreground="{StaticResource MutedBrush}" Text="{Binding Name}" />
                          <TextBlock Grid.Column="1" TextWrapping="Wrap" Text="{Binding Value}" />
                        </Grid>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
              </ScrollViewer>
            </TabItem>
          </TabControl>
        </Border>

        <GridSplitter Grid.Column="3" Style="{StaticResource PaneGridSplitterStyle}" ResizeDirection="Columns" />

        <Border Grid.Column="4" Style="{StaticResource PaneBorderStyle}" Margin="0">
          <TabControl Style="{StaticResource WorksurfaceTabControlStyle}">
            <TabItem Header="Current Focus">
              <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                  <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Current History Focus" />
                  <TextBlock Margin="0,4,0,0" FontWeight="SemiBold" Text="{Binding SelectionHeading}" />
                  <TextBlock Margin="0,2,0,8" FontSize="11" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Text="{Binding SelectionSummary}" />
                  <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding SelectedEntryRows}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Grid Margin="0,0,0,6">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Foreground="{StaticResource MutedBrush}" Text="{Binding Name}" />
                          <TextBlock Grid.Column="1" TextWrapping="Wrap" Text="{Binding Value}" />
                        </Grid>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
              </ScrollViewer>
            </TabItem>
            <TabItem Header="Controls">
              <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                  <TextBlock Style="{StaticResource SectionTitleStyle}" Text="History Controls" />
                  <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding ControlSections}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="8" Background="#FCFDFE" BorderBrush="#D0D8E1" BorderThickness="1">
                          <StackPanel>
                            <TextBlock FontWeight="SemiBold" Text="{Binding Title}" />
                            <TextBlock Margin="0,2,0,6" FontSize="11" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Text="{Binding Summary}" />
                            <ItemsControl ItemsSource="{Binding Rows}">
                              <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                  <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                      <ColumnDefinition Width="118" />
                                      <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Foreground="{StaticResource MutedBrush}" Text="{Binding Name}" />
                                    <TextBlock Grid.Column="1" TextWrapping="Wrap" Text="{Binding Value}" />
                                  </Grid>
                                </DataTemplate>
                              </ItemsControl.ItemTemplate>
                            </ItemsControl>
                          </StackPanel>
                        </Border>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
              </ScrollViewer>
            </TabItem>
          </TabControl>
        </Border>
      </Grid>
    </DockPanel>
  </DataTemplate>
</ResourceDictionary>
