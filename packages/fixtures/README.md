# fixtures

- `label-jobs`: ジョブの正常系 / 異常系サンプル
  `valid-minimal.json` は `pdf-a4-proof` を使う canonical proof fixture
  `valid-windows-spooler.json` は Windows spooler skeleton 向け fixture
- `importer`: Excel/CSV 正規化前提の列定義サンプル
- `golden`: SVG / PDF の期待出力
